@model QuokkaServiceRegistry.Models.CatalogService

@{
    ViewData["Title"] = "Edit Service";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1>@ViewData["Title"]</h1>
                <div>
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-info">
                        <i class="fas fa-eye"></i> View Details
                    </a>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Back to List
                    </a>
                </div>
            </div>

            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="Id" />
                
                <div class="row">
                    <div class="col-lg-8">
                        <!-- Basic Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-info-circle"></i> Basic Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-8">
                                        <div class="form-group mb-3">
                                            <label asp-for="Name" class="form-label"></label>
                                            <input asp-for="Name" class="form-control" placeholder="Enter service name" />
                                            <span asp-validation-for="Name" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="form-group mb-3">
                                            <div class="form-check mt-4">
                                                <input asp-for="IsPropriety" class="form-check-input" />
                                                <label asp-for="IsPropriety" class="form-check-label">
                                                    Is Proprietary Software
                                                </label>
                                            </div>
                                            <span asp-validation-for="IsPropriety" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Vendor Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-building"></i> Vendor Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="form-group mb-3">
                                    <label class="form-label">Select Vendor</label>
                                    <select class="form-select" name="VendorId" asp-items="ViewBag.VendorId">
                                        <option value="">-- Select Vendor --</option>
                                    </select>
                                    <span asp-validation-for="Vendor" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <!-- License Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-certificate"></i> License Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="form-group mb-3">
                                    <label class="form-label">Select License</label>
                                    <select class="form-select" name="LicenseId" asp-items="ViewBag.LicenseId">
                                        <option value="">-- Select License --</option>
                                    </select>
                                    <span asp-validation-for="License" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Hosting Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-server"></i> Hosting Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-4">
                                        <div class="form-group mb-3">
                                            <label asp-for="HostingType" class="form-label">Hosting Type</label>
                                            <select asp-for="HostingType" class="form-select" asp-items="Html.GetEnumSelectList<CatalogHostingType>()"></select>
                                            <span asp-validation-for="HostingType" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="form-group mb-3">
                                            <label asp-for="HostingCountry" class="form-label">Hosting Country</label>
                                            <input asp-for="HostingCountry" class="form-control" />
                                            <span asp-validation-for="HostingCountry" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="form-group mb-3">
                                            <label asp-for="SaasRegionReference" class="form-label">SaaS Region (Optional)</label>
                                            <input asp-for="SaasRegionReference" class="form-control" />
                                            <span asp-validation-for="SaasRegionReference" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Lifecycle Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-project-diagram"></i> Lifecycle Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="form-group mb-3">
                                    <label class="form-label">Select Lifecycle</label>
                                    <select class="form-select" name="LifecycleId" asp-items="ViewBag.LifecycleId">
                                        <option value="">-- Select Lifecycle --</option>
                                    </select>
                                    <span asp-validation-for="Lifecycle" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-4">
                        <!-- Current Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-info"></i> Current Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <strong>Service ID:</strong> @Model.Id
                                </div>
                                @if (Model.Vendor != null)
                                {
                                    <div class="mb-3">
                                        <strong>Current Vendor:</strong><br>
                                        <span class="badge bg-info">@Model.Vendor.Name</span>
                                    </div>
                                }
                                @if (Model.License != null)
                                {
                                    <div class="mb-3">
                                        <strong>Current License:</strong><br>
                                        <span class="badge bg-secondary">@Model.License.Name</span>
                                    </div>
                                }
                            </div>
                        </div>

                        <!-- Optional Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-plus-circle"></i> Optional Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <!-- Subscription -->
                                <div class="form-group mb-3">
                                    <label class="form-label">Subscription (Optional)</label>
                                    <select class="form-select" name="SubscriptionId" asp-items="ViewBag.SubscriptionId">
                                        <option value="">-- No Subscription --</option>
                                    </select>
                                    <span asp-validation-for="Subscription" class="text-danger"></span>
                                </div>

                                <!-- GDPR Register -->
                                <div class="form-group mb-3">
                                    <label class="form-label">GDPR Register (Optional)</label>
                                    <select class="form-select" name="GdprRegisterId" asp-items="ViewBag.GdprRegisterId">
                                        <option value="">-- No GDPR Register --</option>
                                    </select>
                                    <span asp-validation-for="GdprRegister" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Audit Information -->
                        <div class="card mb-4">
                            <div class="card-header">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-history"></i> Audit Information
                                </h5>
                            </div>
                            <div class="card-body">
                                <small class="text-muted">
                                    Last modified: Just now<br>
                                    Created: @DateTime.Now.ToString("yyyy-MM-dd")
                                </small>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Form Actions -->
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex justify-content-between">
                                    <div>
                                        <a asp-action="Index" class="btn btn-secondary">
                                            <i class="fas fa-times"></i> Cancel
                                        </a>
                                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-info">
                                            <i class="fas fa-eye"></i> View Details
                                        </a>
                                    </div>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-save"></i> Save Changes
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
}