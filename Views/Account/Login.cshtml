@{
    ViewData["Title"] = "Login";
    Layout = "_Layout";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <div class="card shadow mt-5">
                <div class="card-body">
                    <div class="text-center mb-4">
                        <h2 class="h4 text-gray-900">QuokkaServiceRegistry</h2>
                        <p class="text-muted">Please sign in to access the application</p>
                    </div>

                    @if (ViewData["Error"] != null)
                    {
                        <div class="alert alert-danger" role="alert">
                            <i class="fas fa-exclamation-triangle"></i> @ViewData["Error"]
                        </div>
                    }

                    @if (ViewData["GoogleAuthAvailable"] != null && (bool)ViewData["GoogleAuthAvailable"])
                    {
                        <!-- Google Login -->
                        <div class="mb-4">
                            <a href="@Url.Action("GoogleLogin", "Account", new { returnUrl = ViewData["ReturnUrl"] })" 
                               class="btn btn-danger btn-block w-100 d-flex align-items-center justify-content-center">
                                <i class="fab fa-google me-2"></i> Sign in with Google
                            </a>
                            <small class="text-muted d-block mt-2 text-center">
                                <i class="fas fa-info-circle"></i> You'll be able to choose your Google account
                            </small>
                        </div>
                    }

                    @if (ViewData["AllowLocalBypass"] != null && (bool)ViewData["AllowLocalBypass"])
                    {
                        @if (ViewData["GoogleAuthAvailable"] != null && (bool)ViewData["GoogleAuthAvailable"])
                        {
                            <hr>
                        }
                        else
                        {
                            <div class="alert alert-info mb-4" role="alert">
                                <i class="fas fa-info-circle"></i> Google authentication is not configured. Using local admin login only.
                            </div>
                        }
                        <div class="text-center mb-3">
                            <small class="text-muted">Development Mode Only</small>
                        </div>

                        <!-- Local Admin Login Form -->
                        <form asp-action="LocalLogin" method="post">
                            <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />
                            
                            <div class="mb-3">
                                <label for="username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="username" name="username" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="password" class="form-label">Password</label>
                                <input type="password" class="form-control" id="password" name="password" required>
                            </div>
                            
                            <button type="submit" class="btn btn-secondary btn-block w-100">
                                <i class="fas fa-user"></i> Local Admin Login
                            </button>
                        </form>
                    }
                </div>
            </div>

            <div class="text-center mt-3">
                <small class="text-muted">
                    Access is restricted to authorized users only.<br>
                    Contact your administrator if you need access.
                </small>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Auto-focus on username field if local bypass is enabled
        $(document).ready(function() {
            if ($('#username').length) {
                $('#username').focus();
            }
        });
    </script>
}